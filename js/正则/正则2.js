
// html to string  没测呢
// html.replace(/^<p[^<>]*>([\s\S]*?)<\/p>$/g, '$1')
// .replace(/<br\s?\/>/g, '\n')
// .replace(/<p[^<>]*>(&nbsp;|\s)*<\/p>/g, '\n')
// .replace(/<p[^<>]*>([\s\S]*?)<\/p>/g, '$1\n')
// .replace(/<[^<>]+>/g, '')


// 只保留【字母数字】
// const b = ' After  that, she was named the most valuable athlete(运动员)   -0A9'.replaceAll(/[\W_]/g, '');
// console.log(b);
